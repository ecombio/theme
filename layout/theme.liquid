<!doctype html>
<html lang="{{ request.locale.iso_code }}">
  <head>
    {{ 'base.css'                  | asset_url | stylesheet_tag }}
    {{ 'theme.css'                 | asset_url | stylesheet_tag }}
    {{ 'quick-view.css'            | asset_url | stylesheet_tag }}
    {{ 'cart-drawer.css'           | asset_url | stylesheet_tag }}
    {{ 'cart-recommendations.css'  | asset_url | stylesheet_tag }}
    {{ 'article-card.css'          | asset_url | stylesheet_tag }}
    {{ 'product-card.css'          | asset_url | stylesheet_tag }}
    {{ 'collection-layout.css'     | asset_url | stylesheet_tag }}
    {{ 'compare-bar.css'           | asset_url | stylesheet_tag }}
    {{ 'section-accordion.css'     | asset_url | stylesheet_tag }}
    {{ 'related-products.css'      | asset_url | stylesheet_tag }}

    {% render 'css-variables' %}

    {% unless settings.type_primary_font.system? %}
      <link rel="preconnect" href="https://fonts.shopifycdn.com" crossorigin>
      {{ settings.type_primary_font | font_url | preload_tag: as: 'font', crossorigin: 'anonymous' }}
    {% endunless %}

    {{ 'critical.css' | asset_url | stylesheet_tag: preload: true }}

    {% render 'meta-tags' %}

    {% if settings.favicon != blank %}
      <link rel="icon" type="image/png"
            href="{{ settings.favicon | image_url: width: 32, height: 32 }}">
    {% endif %}

    {{ content_for_header }}

    <style>
#header-group-wrapper {
  position: fixed !important;
  top: 0 !important;
  left: 0 !important;
  width: 100% !important;
  z-index: 2147483647 !important;
  background: #ffffff !important;
  box-shadow: 0 2px 8px rgba(0,0,0,0.1) !important;
}

      #header-group-sticky-spacer {
        height: 68px;
      }

      @media (max-width: 1023px) { #header-group-sticky-spacer { height: 64px; } }
      @media (max-width: 599px) { #header-group-sticky-spacer { height: 56px; } }

      /* NUCLEAR - destroy stacking traps */
      main, .shopify-section, section, .page-width, [class*="section"] {
        overflow: visible !important;
        transform: none !important;
        filter: none !important;
        contain: none !important;
      }
    </style>
  </head>

  <body class="{{ settings.theme_preset | default: 'ecombio' }}" data-header-layout="{{ settings.header_layout }}">

    <div id="header-group-wrapper">
      {% sections 'header-group' %}
    </div>
    <div id="header-group-sticky-spacer"></div>

    <main role="main">
      {{ content_for_layout }}
    </main>

    <footer role="contentinfo">
      <div class="page-width--footer">
        {% sections 'footer-group' %}
      </div>
    </footer>

    {{- 'base.js' | asset_url | script_tag }}
    {{- 'theme.js' | asset_url | script_tag }}
    <!-- other scripts... -->

    <script>
      // Final boss - force it every scroll
      const header = document.getElementById('header-group-wrapper');
      if (header) {
        window.addEventListener('scroll', () => {
          header.style.position = 'sticky';
          header.style.top = '0';
        }, {passive: true});
      }
    </script>

  </body>
</html>