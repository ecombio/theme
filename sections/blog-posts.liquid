{% comment %}
  Blog Posts Section - Locked to /blogs/articles (using native tags)
  Best version for production
{% endcomment %}

{{ 'blog-posts.css' | asset_url | stylesheet_tag }}
{{ 'blog-posts.js' | asset_url | script_tag }}

{% assign target_blog = blogs['articles'] %}

<div class="blog-posts-section section-{{ section.id }}">
  
  {% if section.settings.heading != blank %}
    <div class="blog-posts-header">
      <h2 class="blog-posts-title">{{ section.settings.heading }}</h2>
    </div>
  {% endif %}

  {% assign current_tag = request.params.tag %}

  {% comment %} Tag Filter Bar {% endcomment %}
  {% assign all_tags = target_blog.all_tags %}

  {% if all_tags.size > 0 %}
    <div class="tag-filter-bar" id="tag-filter-bar">
      <a href="{{ target_blog.url }}" class="tag-pill {% if current_tag == blank %}active{% endif %}">
        All
      </a>
      {% for tag in all_tags %}
        <a href="{{ target_blog.url }}/tagged/{{ tag | handle }}" 
           class="tag-pill {% if current_tag == tag %}active{% endif %}">
          {{ tag }}
        </a>
      {% endfor %}
    </div>
  {% endif %}

  {% comment %} Articles Grid {% endcomment %}
  <div class="blog-posts-grid">
    {% for article in target_blog.articles limit: section.settings.limit %}
      
      {% if current_tag == blank or article.tags contains current_tag %}
        <article class="blog-post-card">
          
          {% if article.image %}
            <a href="{{ article.url }}" class="blog-post-image">
              <img 
                src="{{ article.image | image_url: width: 800, height: 520, crop: 'center' }}" 
                alt="{{ article.image.alt | escape }}"
                loading="lazy">
            </a>
          {% endif %}

          <div class="blog-post-content">
            <h3 class="blog-post-title">
              <a href="{{ article.url }}">{{ article.title }}</a>
            </h3>

            {% if article.excerpt != blank %}
              <p class="blog-post-excerpt">{{ article.excerpt | strip_html | truncate: 135 }}</p>
            {% endif %}

            {% if article.tags.size > 0 %}
              <div class="blog-post-tags">
                {% for tag in article.tags %}
                  <a href="{{ target_blog.url }}/tagged/{{ tag | handle }}" class="tag-pill small">
                    {{ tag }}
                  </a>
                {% endfor %}
              </div>
            {% endif %}

            <div class="blog-post-meta">
              <time datetime="{{ article.published_at | date: '%Y-%m-%d' }}">
                {{ article.published_at | date: '%b %d, %Y' }}
              </time>
              <a href="{{ article.url }}" class="read-more-link">Read article →</a>
            </div>
          </div>

        </article>
      {% endif %}
    {% endfor %}
  </div>

</div>

{% schema %}
{
  "name": "Blog Posts (Articles)",
  "settings": [
    {
      "type": "text",
      "id": "heading",
      "label": "Section Heading",
      "default": "Latest Articles"
    },
    {
      "type": "range",
      "id": "limit",
      "min": 3,
      "max": 24,
      "step": 1,
      "default": 12,
      "label": "Max articles to show"
    }
  ],
  "presets": [
    {
      "name": "Blog Posts from Articles",
      "category": "Blog"
    }
  ]
}
{% endschema %}