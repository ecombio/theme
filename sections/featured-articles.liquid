.featured-articles {
  padding-block: 4rem;
  font-family: var(--font-primary--family);
  color: var(--color-foreground);
  background-color: var(--color-background);
}

.featured-articles__container {
  margin-inline: auto;
  padding-inline: var(--section-padding);
}

/* Section width variants — scoped locally since this theme has no
   global default-width / full-width / custom-width utility classes */
.featured-articles__default-width .featured-articles__container {
  max-width: 1280px;
}

.featured-articles__full-width .featured-articles__container {
  max-width: none;
}

.featured-articles__custom-width .featured-articles__container {
  max-width: var(--custom-width);
}

.featured-articles__heading {
  font-size: clamp(1.5rem, 1.2rem + 1.2vw, 2.25rem);
  line-height: 1.2;
  margin: 0 0 2rem;
}

/* Track wrapper + hover-only arrows */
.featured-articles__track-wrapper {
  position: relative;
}

.featured-articles__nav-btn {
  position: absolute;
  top: 50%;
  transform: translateY(-50%);
  z-index: 2;
  display: inline-flex;
  align-items: center;
  justify-content: center;
  width: 42px;
  height: 42px;
  border-radius: 50%;
  border: 1px solid color-mix(in srgb, var(--color-foreground) 15%, transparent);
  background: var(--color-background);
  color: var(--color-foreground);
  cursor: pointer;
  opacity: 0;
  visibility: hidden;
  transition: opacity var(--transition-fast), background-color var(--transition-fast), border-color var(--transition-fast);
}

.featured-articles__track-wrapper:hover .featured-articles__nav-btn,
.featured-articles__track-wrapper:focus-within .featured-articles__nav-btn {
  opacity: 1;
  visibility: visible;
}

.featured-articles__nav-btn:hover:not(:disabled) {
  background: color-mix(in srgb, var(--color-foreground) 6%, transparent);
  border-color: var(--color-blue);
}

.featured-articles__nav-btn:disabled {
  opacity: 0 !important;
  visibility: hidden !important;
}

.featured-articles__nav-btn--prev {
  left: 0.5rem;
}

.featured-articles__nav-btn--next {
  right: 0.5rem;
}

/* Track */
.featured-articles__track {
  display: grid;
  grid-auto-flow: column;
  grid-auto-columns: 85%;
  gap: 1.25rem;
  overflow-x: auto;
  scroll-snap-type: x mandatory;
  scroll-behavior: smooth;
  padding: 0;
  margin: 0;
  list-style: none;
  -webkit-overflow-scrolling: touch;
  scrollbar-width: none;
}

.featured-articles__track::-webkit-scrollbar {
  display: none;
}

@media screen and (min-width: 750px) {
  .featured-articles__track {
    grid-auto-columns: calc(50% - 0.625rem);
  }
}

@media screen and (min-width: 990px) {
  .featured-articles__track {
    grid-auto-columns: calc(33.333% - 0.834rem);
  }
}

.featured-articles__slide {
  scroll-snap-align: start;
}

/* Card */
.featured-articles__card {
  display: block;
  color: inherit;
  text-decoration: none;
}

.featured-articles__image-wrapper {
  aspect-ratio: 4 / 3;
  overflow: hidden;
  border-radius: var(--radius-base);
  background: color-mix(in srgb, var(--color-foreground) 4%, transparent);
}

.featured-articles__image {
  width: 100%;
  height: 100%;
  object-fit: cover;
  display: block;
  transition: transform var(--transition-slow);
}

.featured-articles__card:hover .featured-articles__image {
  transform: scale(1.04);
}

.featured-articles__image--placeholder svg {
  width: 100%;
  height: 100%;
}

.featured-articles__title {
  font-size: 1.15rem;
  line-height: 1.35;
  margin: 1rem 0 0;
  transition: color var(--transition-fast);
}

.featured-articles__card:hover .featured-articles__title {
  color: var(--color-blue);
}