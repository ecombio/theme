{%- comment -%} Usage: {% render 'product-bundle', bundle_product: product %} {%- endcomment -%}

{{ 'product-bundle.css' | asset_url | stylesheet_tag }}

<div class="product-bundle__grid">
  {%- for item in bundle_product.metafields.custom.product_bundles.value -%}
    {%- render 'product-card', product: item -%}
  {%- endfor -%}
</div>