{{ 'image-grid.css' | asset_url | stylesheet_tag }}

<div class="image-grid-section" style="background-color: {{ section.settings.bg_color }};">
  {% if section.settings.heading != blank %}
    <h2 class="image-grid-heading">{{ section.settings.heading | escape }}</h2>
  {% endif %}

  <div class="image-grid">
    {% for block in section.blocks %}
      {% if block.type == 'tile' %}
        <a
          href="{{ block.settings.link | default: '#' }}"
          class="image-grid__tile"
          style="background-color: {{ block.settings.tile_color }};"
          {{ block.shopify_attributes }}
        >
          <div class="image-grid__media">
            {% if block.settings.image != blank %}
              {{
                block.settings.image
                | image_url: width: 600
                | image_tag:
                  loading: 'lazy',
                  sizes: '(min-width: 990px) 25vw, (min-width: 750px) 33vw, 50vw',
                  widths: '150, 300, 450, 600',
                  class: 'image-grid__img'
              }}
            {% else %}
              {{ 'image' | placeholder_svg_tag: 'image-grid__img placeholder' }}
            {% endif %}
          </div>
          {% if block.settings.label != blank %}
            <p class="image-grid__label">{{ block.settings.label | escape }}</p>
          {% endif %}
        </a>
      {% endif %}
    {% endfor %}
  </div>
</div>

{% schema %}
{
  "name": "Image grid",
  "tag": "section",
  "class": "section",
  "settings": [
    {
      "type": "text",
      "id": "heading",
      "label": "Heading",
      "default": "Shop our most wanted"
    },
    {
      "type": "color",
      "id": "bg_color",
      "label": "Section background",
      "default": "#F5F6F7"
    },
    {
      "type": "range",
      "id": "columns_desktop",
      "label": "Columns on desktop",
      "min": 2,
      "max": 6,
      "step": 1,
      "default": 4
    },
    {
      "type": "range",
      "id": "columns_tablet",
      "label": "Columns on tablet",
      "min": 1,
      "max": 4,
      "step": 1,
      "default": 3
    },
    {
      "type": "range",
      "id": "columns_mobile",
      "label": "Columns on mobile",
      "min": 1,
      "max": 3,
      "step": 1,
      "default": 2
    },
    {
      "type": "range",
      "id": "gap",
      "label": "Grid gap (px)",
      "min": 4,
      "max": 32,
      "step": 2,
      "default": 16
    }
  ],
  "blocks": [
    {
      "type": "tile",
      "name": "Tile",
      "settings": [
        {
          "type": "image_picker",
          "id": "image",
          "label": "Image"
        },
        {
          "type": "text",
          "id": "label",
          "label": "Label",
          "default": "Category name"
        },
        {
          "type": "url",
          "id": "link",
          "label": "Link"
        },
        {
          "type": "color",
          "id": "tile_color",
          "label": "Tile background color",
          "default": "#E9F687"
        }
      ]
    }
  ],
  "presets": [
    {
      "name": "Image grid",
      "blocks": [
        { "type": "tile", "settings": { "label": "Great deals" } },
        { "type": "tile", "settings": { "label": "iPhone" } },
        { "type": "tile", "settings": { "label": "MacBook" } },
        { "type": "tile", "settings": { "label": "iPad" } },
        { "type": "tile", "settings": { "label": "Video game consoles" } },
        { "type": "tile", "settings": { "label": "Android" } },
        { "type": "tile", "settings": { "label": "Windows Laptops" } },
        { "type": "tile", "settings": { "label": "Airpods" } }
      ]
    }
  ]
}
{% endschema %}
