{% comment %}
  ╔══════════════════════════════════════════════════════════╗
  ║  sections/product-card-single.liquid                     ║
  ║  Purpose: sections API wrapper for product-card snippet  ║
  ║                                                          ║
  ║  Used exclusively by product-recently-viewed.liquid:     ║
  ║    fetch(`/products/${handle}?sections=product-card-single`)
  ║                                                          ║
  ║  Returns a fully-rendered product-card.liquid fragment   ║
  ║  so Recently Viewed cards are identical in markup and    ║
  ║  styling to every other product-card on the page.        ║
  ║                                                          ║
  ║  Do NOT add this section to any template or page order.  ║
  ╚══════════════════════════════════════════════════════════╝
{% endcomment %}

{%- render 'product-card', product: product -%}
