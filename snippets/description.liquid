{%- comment -%}
  snippets/description.liquid
  Renders the product's video carousel (sourced from the
  custom.product_videos metaobject-list metafield, via snippets/videos.liquid)
  followed by product.description.

  Usage:
    {% render 'description', product: product %}
{%- endcomment -%}

{% render 'videos', product: product %}

{{ product.description }}