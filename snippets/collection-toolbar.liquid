{% comment %}
  Snippet: collection-toolbar
  Renders the filter toggle button, product count, and sort selector.
  Context-aware: collection page vs search page.

  Parameters (all optional — collection variables are global and need no passing):
    - show_filters  {Boolean} — whether to render the filter toggle button
    - active_tab    {String}  — 'products' | 'articles' | 'pages' (search page only)
    - product_count {Number}  — pre-counted product results (search page only)
    - article_count {Number}  — pre-counted article results (search page only)
    - page_count    {Number}  — pre-counted page results (search page only)

  Used in: sections/main-collection.liquid, sections/main-search.liquid
{% endcomment %}

<div class="collection-toolbar">

  {%- if show_filters -%}
    <button
      class="collection-toolbar__filter-btn"
      type="button"
      id="filters-open-btn"
      aria-expanded="false"
      aria-controls="collection-filters"
    >
      <svg width="16" height="16" viewBox="0 0 16 16" fill="none" aria-hidden="true">
        <path d="M2 4h12M4 8h8M6 12h4" stroke="currentColor" stroke-width="1.5" stroke-linecap="round"/>
      </svg>
      Filters
      <span class="collection-toolbar__active-count" id="active-filter-count" aria-live="polite"></span>
    </button>
  {%- endif -%}

  <span class="collection-toolbar__count">
    {%- if search.performed -%}
      {%- if active_tab == 'articles' -%}
        {{ article_count }} {{ article_count | pluralize: 'article', 'articles' }}
      {%- elsif active_tab == 'pages' -%}
        {{ page_count }} {{ page_count | pluralize: 'page', 'pages' }}
      {%- else -%}
        {{ product_count }} {{ product_count | pluralize: 'product', 'products' }}
      {%- endif -%}
    {%- else -%}
      {%- if collection.products_count == collection.all_products_count -%}
        {{ collection.all_products_count }} {{ collection.all_products_count | pluralize: 'product', 'products' }}
      {%- else -%}
        {{ collection.products_count }} of {{ collection.all_products_count }} products
      {%- endif -%}
    {%- endif -%}
  </span>

  {%- unless active_tab == 'pages' -%}
    {% render 'collection-sort' %}
  {%- endunless -%}

</div>