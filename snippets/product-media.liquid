{{ 'product-media.css' | asset_url | stylesheet_tag }}

<div class="product-images">

  <!-- Main Image -->
  <div class="main-image-wrapper">
    {% assign featured = product.selected_or_first_available_variant.featured_image | default: product.featured_image | default: product.images.first %}
    <img
      src="{{ featured | image_url: width: 900 }}"
      alt="{{ featured.alt | escape }}"
      class="main-product-image"
      id="main-product-image"
    >
  </div>

  <!-- Vertical Thumbnails -->
  {% if product.images.size > 1 %}
    <div class="vertical-thumbnails" id="vertical-thumbnails">
      {% for image in product.images %}
        <img
          src="{{ image | image_url: width: 120 }}"
          alt="{{ image.alt | escape }}"
          class="thumbnail{% if image == featured %} active{% endif %}"
          data-media-id="{{ image.id }}"
          data-full-image="{{ image | image_url: width: 900 }}"
          data-alt="{{ image.alt | escape }}"
        >
      {% endfor %}
    </div>
  {% endif %}

</div>