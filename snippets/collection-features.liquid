{% comment %}
  snippets/collection-features.liquid
  Houses the filter sidebar and results count for the collection page.

  Expects:
    collection    {Object} — the Shopify collection object
    results_count {Number} — collection.products_count
{% endcomment %}

{%- render 'collection-filters', collection: collection -%}