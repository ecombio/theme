{%- comment -%}
  ─────────────────────────────────────────────────────────────
  SCHEMA ADDITIONS — card-product feature flags
  ─────────────────────────────────────────────────────────────
  Merge the settings below into the "settings" array in your
  existing main-collection-products.liquid {% schema %} block.
  ─────────────────────────────────────────────────────────────
{%- endcomment -%}

{%- comment -%} PASTE INSIDE YOUR {% schema %} → "settings": [ … ] {%- endcomment -%}

/*
    {
      "type": "header",
      "content": "Product card features"
    },
    {
      "type": "checkbox",
      "id": "enable_quickview",
      "label": "Enable Quick View button",
      "default": true,
      "info": "Shows a Quick View button on hover. Requires a product.quickview.liquid template."
    },
    {
      "type": "checkbox",
      "id": "enable_atc",
      "label": "Enable Add to Cart button",
      "default": true
    },
    {
      "type": "checkbox",
      "id": "enable_compare",
      "label": "Enable Compare checkbox",
      "default": false,
      "info": "Lets shoppers select up to 4 products to compare. Requires a /pages/compare page."
    },
    {
      "type": "checkbox",
      "id": "enable_wishlist",
      "label": "Enable Wishlist button",
      "default": true
    },
    {
      "type": "header",
      "content": "Estimated arrival"
    },
    {
      "type": "checkbox",
      "id": "show_eta",
      "label": "Show estimated arrival on cards",
      "default": false
    },
    {
      "type": "text",
      "id": "eta_label",
      "label": "ETA label prefix",
      "default": "Est. arrival:"
    },
    {
      "type": "range",
      "id": "eta_days_min",
      "label": "Minimum days",
      "min": 1,
      "max": 30,
      "step": 1,
      "default": 3
    },
    {
      "type": "range",
      "id": "eta_days_max",
      "label": "Maximum days",
      "min": 1,
      "max": 60,
      "step": 1,
      "default": 7
    }
*/
