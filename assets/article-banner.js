// article-banner.js — Barebone Article Banner

document.addEventListener('DOMContentLoaded', () => {
  const banners = document.querySelectorAll('.article-banner');

  banners.forEach(banner => {
    // Placeholder for future interactivity
    // (e.g. parallax, animated text, video background, etc.)
    console.log('%c[Article Banner] Initialized', 'color: #888');
  });
});